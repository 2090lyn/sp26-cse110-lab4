1. values added: 20
2. final result: 20
3. You shouldn't use var because it's accessible everywhere in the file, which could cause issues like naming conflicts and scoping issues.

4. values added: 20
5. Error - It can only be accessed inside the if-statement scope, not outside of it.

6. Error - it's a const variable, so it's set and can't change, which is why an error was called.
7. Error - It can only be accessed inside the if-statement scope, not outside of it.